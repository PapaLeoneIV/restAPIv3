version: "1"
packages:
  - name: "db"
    path: "db"
    queries: "./db/queries"
    schema: "./db/schema.sql"
    engine: "postgresql"
    emit_json_tags: true
    emit_prepared_queries: false
    emit_interface: false